using System;

class Program
{
    static void Main()
    {
        //compile time - the compiler calculates such expressions during compile time not runtime
        //e.g.
        int r = (150 - 20 / 2 +5 // r = 70)
       //eeach expression returns a type
       //also e.g. Math.PI * r will compile on runtime
       //expressions of type int are calculated at runtime
       //expressions of bool type are calculated at runtime -  compilatora nqma kak da znae kakvi
       //stoinosti na promenlivite shte ima predi da se vyvedat
    }
}
